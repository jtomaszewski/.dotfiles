1. download apps:
  - chrome
  - spectacle
  - magicprefs
  - spotify
  - iterm
  - franz
  - atom

2. install packages:
  - xcode dev tools
  - brew

brew install hub wget tmux fish rbenv

generate/import ssh key

git co dotfiles into ~/.dotfiles , and run ./update.sh


